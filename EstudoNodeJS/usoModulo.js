const moduloPrimeiro = require('./modulo1.js') //esta função permite o uso do modulo ja criado

const soma = moduloPrimeiro.calculo

soma(4, 6);
soma(3, 2);

const subtrair = moduloPrimeiro.subtracao

subtrair(10, 2);
subtrair(8, 4);